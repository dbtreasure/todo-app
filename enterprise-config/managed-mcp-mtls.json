{
  "mcpServers": {
    "internal-api": {
      "command": "node",
      "args": ["internal-mcp-server/dist/index.js"],
      "env": {
        "API_URL": "https://api.internal.corp.example.com",
        "CLIENT_CERT": "${CLAUDE_CODE_CLIENT_CERT}",
        "CLIENT_KEY": "${CLAUDE_CODE_CLIENT_KEY}",
        "CA_CERT": "${NODE_EXTRA_CA_CERTS}"
      }
    }
  }
}
