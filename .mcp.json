{
  "mcpServers": {
    "user-service": {
      "command": "uv",
      "args": ["run", "mcp-server/user_service.py"],
      "env": {
        "INTERNAL_API_URL": "${INTERNAL_API_URL}",
        "INTERNAL_API_TOKEN": "${INTERNAL_API_TOKEN}"
      }
    }
  }
}
